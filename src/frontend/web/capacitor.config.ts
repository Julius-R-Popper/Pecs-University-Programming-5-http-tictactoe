import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fm91u5.tictactoe',
  appName: 'TicTacToe-FM91U5',
  webDir: 'dist/web/browser'
};

export default config;
